{
    "type": "object",
    "required": [
        "function",
        "field",
        "pattern"
    ],
    "additionalProperties": false,
    "properties": {
        "function": {
            "type": "string",
            "enum": [
                "regexp"
            ]
        },
        "field": {
            "type": "string",
            "pattern": "^.+$"
        },
        "pattern": {
            "type": "string",
            "pattern": "^.+$"
        },
        "replace": {
            "type": "string",
            "pattern": "^.+$"
        }
    }
}
