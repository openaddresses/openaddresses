{
    "type": "object",
    "required": [
        "function",
        "field",
        "field_to_remove"
    ],
    "additionalProperties": false,
    "properties": {
        "function": {
            "type": "string",
            "enum": [
                "remove_prefix"
            ]
        },
        "field": {
            "type": "string",
            "pattern": "^.+$"
        },
        "field_to_remove": {
            "type": "string",
            "pattern": "^.+$"
        }
    }
}
