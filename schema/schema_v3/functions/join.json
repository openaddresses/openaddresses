{
    "type": "object",
    "required": [
        "function",
        "fields"
    ],
    "additionalProperties": false,
    "properties": {
        "function": {
            "type": "string",
            "enum": [
                "join"
            ]
        },
        "fields": {
            "type": "array",
            "minItems": 2,
            "items": {
                "type": "string",
                "pattern": "^.+$"
            }
        },
        "separator": {
            "type": "string",
            "pattern": "^.+$"
        }
    }
}
