{
  "description": "top-level parcel conform schema",
  "type": "object",
  "required": ["pid"],
  "additionalProperties": false,
  "properties": {
    "format": { "type": "string", "enum": ["gdb", "geojson", "gpkg", "shapefile"] },
    "srs": { "type": "string", "pattern": "EPSG:[0-9]+" },
    "file": { "type": "string" },
    "layer": {
      "description": "specifies a layer to use in the GDB",
      "oneOf": [{ "type": "integer" }, { "type": "string" }]
    },
    "encoding": { "type": "string" },
    "notes": { "type": "string" },
    "size": {
      "description": "processing resource tier for unusually large sources (e.g. nationwide datasets). Omit unless the source has been shown to need more than the default scratch disk. See CONTRIBUTING.md",
      "type": "string",
      "enum": ["standard", "large"]
    },
    "pid": {
      "description": "unique identifier for the parcel row",
      "$ref": "../util/functions/standard_functions.json"
    }
  }
}
