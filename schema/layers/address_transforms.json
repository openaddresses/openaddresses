{
    "description": "top-level address validate schema",
    "type": "object",
    "required": [
        "transforms"
    ],
    "additionalProperties": false,
    "properties": {
        "transforms": {
            "type": "array",
            "description": "Transforms to perform on the source data",
            "items": {
                "$ref": "./address_transform.json"
            }
        }
    }
}
