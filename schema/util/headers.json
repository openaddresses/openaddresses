{
    "type": "object",
    "description": "Additional HTTP request headers to send when downloading this source. Applies to protocol http and ESRI; ignored for ftp. Not to be confused with conform.headers, which selects the CSV row holding column names. Never put credentials or API keys here — sources are public.",
    "additionalProperties": { "type": "string" }
}
