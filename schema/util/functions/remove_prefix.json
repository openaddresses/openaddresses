{
    "description": "remove_prefix function definition",
    "type": "object",
    "required": [
        "function",
        "field",
        "field_to_remove"
    ],
    "additionalProperties": false,
    "properties": {
        "function": {
            "type": "string",
            "enum": [
                "remove_prefix"
            ]
        },
        "field": {
            "type": "string"
        },
        "field_to_remove": {
            "type": "string"
        }
    }
}
